name: routine_application
description: Premium skincare and wellness app.
publish_to: 'none'
version: 0.3.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  go_router: ^14.2.7
  supabase_flutter: ^2.6.0
  flutter_local_notifications: ^17.2.3
  flutter_dotenv: ^5.1.0
  camera: ^0.11.0+2
  flutter_typeahead: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
